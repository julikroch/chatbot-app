export enum MessageAuthorEnum {
  User = 'user',
  Bot = 'bot',
}

export enum CommonPathnames {
  Home = '/',
  Admin = '/admin',
}

export enum Logger_Colors {
  trace = 'lightblue',
  info = '#ffa700',
  error = '#d23432',
}
